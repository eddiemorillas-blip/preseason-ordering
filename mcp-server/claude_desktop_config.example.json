{
  "mcpServers": {
    "preseason-ordering": {
      "command": "node",
      "args": ["/FULL/PATH/TO/preseason-ordering/mcp-server/index.js"],
      "env": {
        "DATABASE_URL": "postgresql://user:password@host:port/database"
      }
    }
  }
}
